{% include 'headerSingle' %}

import Foundation
import RTMVVM

struct {{ module_info.name }} {}

extension {{ module_info.name }} {
    class {{ custom_parameters.entity }}Presenter {
        let viewModelSubscriber: VMRepositorySingleSubscriber<{{ module_info.name }}.{{ custom_parameters.entity }}ViewModelSingleFactory>
        private(set) weak var repository: {{ module_info.name }}.{{ custom_parameters.entity }}VMRepositorySingle?

        init(viewModelSubscriber: VMRepositorySingleSubscriber<{{ module_info.name }}.{{ custom_parameters.entity }}ViewModelSingleFactory>, repository: {{ module_info.name }}.{{ custom_parameters.entity }}VMRepositorySingle) {
            self.viewModelSubscriber = viewModelSubscriber
            self.repository = repository
        }
    }
}
