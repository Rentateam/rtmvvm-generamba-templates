{% include 'headerSingle' %}

import UIKit
import Dip

class {{ custom_parameters.entity }}Controller: UIViewController, StoryboardInstantiatable {
    
    var presenter: {{ module_info.name }}.{{ custom_parameters.entity }}Presenter!
    
    override func viewDidLoad() {
        super.viewDidLoad()
//        ...Bind to view model like this...
//        self.kvoController.observe(self.presenter.repository?.viewModel,
//                                   keyPath: #keyPath({{ module_info.name }}.{{ custom_parameters.entity }}ViewModel.<fieldName>),
//                                   options: [.initial, .new, .old],
//                                   action: #selector(self.on<fieldName>Changed))
//      If your model may not exist when the controller is loaded, add this
//        self.kvoController.observe(self.presenter.repository,
//                                   keyPath: #keyPath(TestSingle.TestSingleVMRepositorySingle.viewModel),
//                                   options: [.initial, .new, .old],
//                                   action: #selector(self.onViewModelChanged))
    }
    
    override func didReceiveMemoryWarning() {
        super.didReceiveMemoryWarning()
        // Dispose of any resources that can be recreated.
    }

//    @objc func on<fieldName>Changed() {
//
//    }

//    @objc func onViewModelChanged() {
//          Bind notifications here if the model has changed
//    }
}
