{% include 'headerList' %}
import RTMVVM
import RealmSwift

extension {{ module_info.name }} {
    class {{ custom_parameters.entity }}ViewModelListFactory: ViewModelListFactoryProtocol {
        typealias ViewModel = {{ custom_parameters.entity }}ViewModel
        typealias RealmObject = {{ custom_parameters.entity }}Object

        func getRealmResults(realm: Realm) -> Results<RealmObject> {
            #warning("Add code here")
            return realm.objects(RealmObject.self)
        }

        func createViewModelList(results: Results<RealmObject>) -> [ViewModel] {
            var list = [ViewModel]()
            results.forEach{
                list.append(self.createViewModelSingle(item: $0))
            }
            return list
        }

        func createViewModelSingle(item: RealmObject) -> ViewModel {
            #warning("Add code here")
            return ViewModel()
        }
    }
}
