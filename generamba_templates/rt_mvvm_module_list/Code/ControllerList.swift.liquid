{% include 'headerList' %}

import UIKit
import Dip

class {{ custom_parameters.entity }}Controller: UIViewController, StoryboardInstantiatable {
    
    var presenter: {{ module_info.name }}.{{ custom_parameters.entity }}Presenter!
    
    override func viewDidLoad() {
        super.viewDidLoad()
//        ...Bind to view model like this...
//        self.kvoController.observe(self.presenter.repository,
//                                   keyPath: #keyPath({{ module_info.name }}.{{ custom_parameters.entity }}VMRepositoryList.viewModel),
//                                   options: [.initial, .new, .old],
//                                   action: #selector(self.onViewModelChanged))
    }
    
    override func didReceiveMemoryWarning() {
        super.didReceiveMemoryWarning()
        // Dispose of any resources that can be recreated.
    }

//    @objc func onViewModelChanged() {
//
//    }
}
