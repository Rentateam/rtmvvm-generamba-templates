{% include 'headerList' %}

import Foundation
import RTMVVM

struct {{ module_info.name }} {}

extension {{ module_info.name }} {
    class {{ custom_parameters.entity }}Presenter {
        let viewModelSubscriber: VMRepositoryListSubscriber<{{ module_info.name }}.{{ custom_parameters.entity }}ViewModelListFactory>
        private(set) weak var repository: {{ module_info.name }}.{{ custom_parameters.entity }}VMRepositoryList?
        
        init(viewModelSubscriber: VMRepositoryListSubscriber<{{ module_info.name }}.{{ custom_parameters.entity }}ViewModelListFactory>, repository: {{ module_info.name }}.{{ custom_parameters.entity }}VMRepositoryList) {
            self.viewModelSubscriber = viewModelSubscriber
            self.repository = repository
        }
    }
}
